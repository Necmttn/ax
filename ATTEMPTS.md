# Ingest-speed attempt ledger

Corpus: frozen subset ~98M at `$HOME/.cache/ax-bench/home`. Bench DB: disposable SurrealDB :8531.
Harness: `~/.cache/ax-bench/bench-run.sh <runs> <out>`. Golden (correctness ref): `~/.cache/ax-bench/golden.json`.
Times are wall-clock seconds per ingest; **cold** = 1st (empty→full), **warm** = last (steady-state re-ingest).

| # | change | cold s | warm s (Δ vs base) | gate | kept? | notes |
|---|--------|--------|--------------------|------|-------|-------|
| 000 | baseline (untouched main) | 33 | 49 | ref | n/a | warm > cold; re-derive-everything is the cost |
| 001 | turn-content-blocks incremental via `NOT IN` | 32 | 138 (**+182%**) | FAIL | ✗ revert | unindexed `NOT IN` membership is O(n·m); also under-derived on cold. Lesson: skip-check must be indexed/hash-based |
| 002 | turn-content-blocks incremental via content_hash Map | 24 | 27 (**-46%**) | PASS | ✓ keep | indexed `content_document_source` lookup → JS `Map<source_ref,content_hash>`; UPSERT only new/changed hashes (deterministic ids ⇒ in-place), no blanket DELETE; `AX_REDERIVE_CONTENT=1` forces full. Counts exact-match golden; converges over the 3 runs as the bench expects |
| 003 | turn-analysis incremental via analyzed-turn Set | 26 | 24 (**-11% vs 27**) | PASS | ✓ keep | turns are append-only ⇒ an existing `turn_analysis` row is output-equivalent. Load analyzed turn keys in ONE indexed read (`SELECT turn FROM turn_analysis`, UNIQUE `turn_analysis_turn` index) → JS `Set`; still derive over the full ordered row set (reacts_to lookahead needs session context) but persist only turns not yet analyzed; dropped the blanket `DELETE reacts_to/expresses/turn_analysis/semantic_signal`. `AX_REDERIVE_ANALYSIS=1` forces full reset+re-derive. Verified output-equivalent: incremental 3-run bench → turn_analysis 15201, semantic_signal 21, expresses 438, reacts_to 45; a forced full re-derive on the same DB produced the identical 15201/21/438/45. All golden `.counts` exact-match |

| 004 | PIPELINE_CONCURRENCY 2 → 4 | 22 | 22 (**-8% vs 24**) | PASS | ✓ keep | one-line constant bump in `apps/axctl/src/ingest/stage/runner.ts`; allows 4 stages inside the scheduling semaphore at once (each stage's own internal fan-out unchanged). Runs [22,28,22] over 3 ingests; counts exact-match golden. No DB-contention regression at bench scale. Pure measurement knob, no correctness surface |

| 005 | turn-content-blocks chunkSize 250 → 1000 | 20 | 23 (**+1 vs 22**) | PASS | ✗ revert | counts exact-match golden (correctness PASS) but warm 23s ≥ 22s best ⇒ not kept. turn-content-blocks is already incremental (attempt 002 ⇒ near-no-op on warm: only new/changed-hash turns write), so larger chunks only matter on the cold full-derive path; cold 20s was within bench noise of 22s, no measurable warm win. Runs [20,31,23] (run 2 = noise spike). Reverted to 250. File: `apps/axctl/src/ingest/turn-content-blocks.ts` |

<!-- append one row per attempt below; keep newest at bottom -->

| 006 | claude skip-unchanged source files via per-file (mtime,size) watermark | 21 | 13 (**-41% vs 22**) | PASS | ✓ keep | new `ingest_file_state` table (path UNIQUE + mtime_ms/size, `source_kind='claude_transcript'`) records a per-file watermark. `ingestTranscripts` loads the whole table in ONE indexed read (`SELECT path,mtime_ms,size FROM ingest_file_state WHERE source_kind='claude_transcript'`) into a JS `Map<path,{mtime,size}>`; always `stat`s each candidate; SKIPS parse+write for any file whose (mtime,size) still matches its watermark (its turns/tool_calls/events already persist ⇒ output-equivalent); processes new/changed files then upserts their watermark only after every write for that file succeeds (mid-file failure re-processes next run). NO `NOT IN`. `AX_REDERIVE_CLAUDE=1` forces full re-parse. Runs [21,20,13]: run 1 = cold (no watermarks, all files), runs 2-3 skip unchanged ⇒ warm 13s. All golden `.counts` exact-match. Files: `apps/axctl/src/ingest/transcripts.ts`, `packages/schema/src/schema.surql` |

| 007 | git skip-unchanged repos via per-repo HEAD watermark | 24 | 8 (**-38% vs 13**) | PASS | ✓ keep | reuse the `ingest_file_state` table with `source_kind='git_repo'` to store per-repo `(path, sha=HEAD, since_days)`. `ingestGit` loads all git watermarks in ONE indexed read (`SELECT path,sha,since_days FROM ingest_file_state WHERE source_kind='git_repo'`) into a JS `Map<path,{sha,sinceDays}>`. Per repo: if `HEAD == watermark.sha && sinceDays == watermark.since_days` the repo is unchanged ⇒ its commits/files already persist ⇒ SKIP `fetchCommits` (the `git log` + per-commit `git show --numstat` walk, the dominant warm cost) and pass `commits=[]` to `writeRepo` (cheap repo/checkout/session-link upserts still run so new sessions link on a skipped repo). After a real walk succeeds, UPSERT the watermark to HEAD. A new commit moves HEAD ⇒ fresh walk; a wider `since_days` ⇒ fresh walk. NO `NOT IN`. `AX_REDERIVE_GIT=1` forces a full walk. Schema: made `mtime_ms`/`size` `option<number>` + added `sha`/`since_days` `option<...>` so one table serves both source kinds. Runs [24,21,8]: run 1 cold builds watermarks, runs 2-3 skip ⇒ warm 8s. All golden `.counts` exact-match. Files: `apps/axctl/src/ingest/git.ts`, `packages/schema/src/schema.surql` |

| 009 | pricing skip-unchanged via statement-fingerprint watermark | 20 | 5 (**-17% vs 6**) | PASS | ✓ keep | `refreshModelPricing` re-UPSERTs the **entire 4333-row `agent_model` catalog** every warm run (with `updated_at=time::now()`) even though the catalog is a deterministic function of the cached pricing JSON + built-in constants and is unchanged between warm runs - the classic re-write-everything antipattern. Fix (mirror 008): build the exact UPSERT statements, fingerprint them with `stableDigest(statements.join("\n"),32)` (the `updated_at=time::now()` clause is identical text in every statement ⇒ does not perturb the digest), load the stored pricing watermark from `ingest_file_state` (`source_kind='pricing'`, fixed sentinel path `__pricing__`) in ONE indexed read; if the digest matches ⇒ catalog unchanged ⇒ all 4333 rows already persist ⇒ SKIP the whole UPSERT batch (output-equivalent; only volatile `updated_at` would differ). On any catalog change (or no watermark, or `AX_REDERIVE_PRICING=1`) do the full UPSERT batch then UPSERT the watermark. NO `NOT IN`. No schema change (008's `sha` field already serves). The cheap `loadPricingCatalog` (cached JSON read + parse) still runs; only the 4333-row UPSERT batch is skipped. Runs [20,18,5]: run 1 cold builds watermark, runs 2-3 skip ⇒ warm 5s. `agent_model` not in golden `.counts` but output-equivalence verified directly: forced `AX_REDERIVE_PRICING=1` re-derive on the post-bench DB reproduced the identical 4333 rows; all 9 golden `.counts` still exact-match. File: `apps/axctl/src/ingest/model-pricing.ts` |
| 008 | closure skip-unchanged via input-fingerprint watermark | 25 | 6 (**-25% vs 8**) | PASS | ✓ keep | RE-PROFILE @ 8s found closure (3.4s) now the top warm stage - the classic blanket-DELETE-then-full-re-derive antipattern (like 002/003): every warm run `DELETE later_fixed_by;DELETE suggests_skill;DELETE skill_candidate;DELETE commit_classification;` then re-RELATEs **6346 `later_fixed_by` edges** + 1513 classifications even when commit/touched/session_health are unchanged (git skip-unchanged ⇒ no new commits warm). Fix: after the cheap read of commits/touched/sessionHealth, compute `stableDigest` (32-hex) over the exact derive inputs (commit id\|message\|ts\|repo, touched in\|path, session_health fields) + sinceDays; load the stored closure watermark from `ingest_file_state` (`source_kind='closure'`, fixed sentinel path `__closure__`) in ONE indexed read; if the digest matches ⇒ inputs unchanged ⇒ persisted output identical ⇒ SKIP the blanket DELETE + full re-write entirely (output-equivalent). On any change (or no watermark, or `AX_REDERIVE_CLOSURE=1`) do the full derive + DELETE + write, then UPSERT the watermark. The 3 reads still run (cheap part); only the 6346-edge DELETE + 6346-edge RELATE writes are skipped. NO `NOT IN`. No schema change (007's `sha` field already optional). Runs [25,23,6]: run 1 cold builds watermark, runs 2-3 skip ⇒ warm 6s. Closure dropped from 3403ms → negligible (out of top-8 spans); new top warm stage = subagents (~1.9s). All golden `.counts` exact-match; output-equivalence confirmed via forced full re-derive reproducing identical 1513/6346/58/4. File: `apps/axctl/src/ingest/closure.ts` |

| 010 | subagents skip-unchanged via per-file (mtime,size) watermark | 25 | 3 (**-40% vs 5**) | PASS | ✓ keep | RE-PROFILE @ 5s found `subagents` (~1.9s) the top warm stage. The stage discovers every `~/.claude/projects/.../subagents/agent-*.jsonl`, fully re-parses each via `extractFileWithSessionId`, and re-writes its session/turns/tool_calls/invocations/spawned-edge on every run even though the files are unchanged between warm runs - same re-derive-everything antipattern as 006. Fix (mirror 006 exactly): add `source_kind='claude_subagent'` to the existing `ingest_file_state` table; load all subagent watermarks in ONE indexed read (`SELECT path,mtime_ms,size FROM ingest_file_state WHERE source_kind='claude_subagent'`) into a JS `Map<path,{mtime,size}>`; `stat` each manifest file and SKIP the parent-check + full re-parse + all writes for any file whose (mtime,size) still matches its watermark (its rows already persist ⇒ output-equivalent); process new/changed files then UPSERT the watermark only after every write for that file succeeds (mid-file failure re-processes next run). NO `NOT IN`. No schema change (table + indexes pre-exist from 006; only a new source_kind value). `AX_REDERIVE_SUBAGENTS=1` forces full re-derive. Runs [25,20,3]: run 1 cold, run 2 builds subagent watermarks (subagents depends on claude data), run 3 fully warm ⇒ 3s. All golden `.counts` exact-match (turn 15201, tool_call 4555, agent_event 17549, content_atom 23892, session 81, skill 24, signal 0). File: `apps/axctl/src/ingest/derive-claude-subagents.ts` |
