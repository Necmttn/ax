// AUTO-GENERATED stub - replaced during a custom DuckDB embed build, then
// restored so source builds do not require a platform-specific library.
//
// Wiring note (#791, wave 3 c-binary-embed): once this stub is generated for
// real, wire it up on the apps side via
// `DuckDbLiveWith({ assetPath: DUCKDB_DYLIB })` (`@ax/lib/duckdb`) - NOT a
// bare `DuckDbLive`. `packages/lib` cannot import from `apps/axctl` (this
// file), so `DuckDbLiveWith`'s optional `assetPath` is the seam that lets an
// apps-side consumer hand in this embed without that illegal cross-package
// import. No consumer wires this in yet.
export const DUCKDB_DYLIB: string | undefined = undefined;
