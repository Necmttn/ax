/// <reference types="vite/client" />

// Injected by Vite `define` (see vite.config.ts) from the root package.json
// version. Compile-time constant; safe to reference in any client/server file.
declare const __AX_VERSION__: string;
