# Fleet run archive: sweep2 + fix2 (2026-08-21)

Third bug wave, following hunt #926 (24 bugs) and fix #956 (19 fixed). Sweep
run map #969 (5 audit lanes), fix run map #1001 (8 fix chunks). All codex,
gpt-5.5 medium (cost-core, fixwave, staleness-models at high). Every finding
adversarially verified by the fable orchestrator in code before filing; every
fix chunk gated (diff review, suite re-run, rebase) before serial merge.

## Result

29 verified bugs found (#970-#1000), all fixed across 8 chunks + a fix-forward,
all merged same-day, all issues closed. Released as v0.40.2.

## Dogfood (fed the sweep)

Source build v0.40.0+14 installed over the local binary (codesigned). The three
first-wave cutovers ran clean: sidecar sig migration LOSSLESS (39 decisions
preserved, backup at ~/.ax/judgment.sqlite.bak-pre-942-migration), full
run-evidence re-derive (370k rows) + FTS v3 rebuild in one 62s warm ingest,
then back to ~31s steady state. Recall (full text) + repriced cost views
verified. Surfaced #970 (workflow proposal sig mismatch, one-time noise) and
#971 (unattributed collapse warning).

## Sweep ledger (#969)

| lane | findings | issues |
|---|---|---|
| audit2-hooks-dojo | 6 | #974 #975 #976 #977 #978 #979 |
| audit2-dashboard-studio | 6 | #980 #981 #982 #983 #984 #985 |
| audit2-segment-watermark | 2 | #986 #987 |
| audit2-mcp-otel-profile | 5 | #988 #989 #990 #991 #992 |
| audit2-fixwave (re-audit of the FIX wave) | 8 | #993 #994 #995 #996 #997 #998 #999 #1000 |
| dogfood | 2 | #970 #971 |

The fixwave lane was the highest-value: it found 8 incomplete-fix regressions
in the just-merged #956 wave, including a TOCTOU in the orchestrator's own #961
stale-claim addition (#995) and five silent-partial / mixed-model cost surfaces
that #966 only half-fixed (#997 #998 #999 #1000 + the routing-impact one).

## Fix ledger (#1001)

| chunk | PR | issues |
|---|---|---|
| fix2-inspect-retag | #1002 | #994 |
| fix2-segment | #1004 | #986 #987 |
| fix2-query-scope | #1005 | #980 #983 #984 #993 |
| fix2-staleness-models | #1006 | #995 #996 |
| fix2-dojo-quota-nav | #1007 | #976 #977 #978 #981 #982 #985 |
| fix2-hooks-advice | #1008 | #974 #975 #979 |
| fix2-otel-community-profile-mcp | #1009 | #970 #988 #989 #990 #991 #992 |
| fix2-cost-core | #1010 | #971 #997 #998 #999 #1000 |
| (fix-forward) | #1012 | check:no-node-fs gate |

## Orchestrator gate findings (beyond pane reports)

- **The load-bearing lesson (#1012):** #979's ledger-rotation fix imported
  `node:path` in advice-stage.ts (an ingest stage). The per-chunk suite + tsc
  were green, so it merged - then main CI's `verify` job failed on
  `check:no-node-fs`. Fix-forward #1012 (posixPath). Per-chunk gates must run
  the repo verify gates (check:no-node-fs / timestamp-cast / raw-numeric-cast),
  not only touched suites. This wave's per-chunk gates ran only the latter.
- **#995 fix quality:** the ownership-token reclaim narrows but the underlying
  best-effort drive relies on downstream withIngestLock as the real backstop;
  accepted with the re-read-before-remove guard, noted.
- **New lead #1011:** the #971 per-table collapse warning surfaced
  otel_metric_point at 4,755 duplicate-id collapses per warm ingest - filed for
  a writer ruling (designed re-emit vs id-collision).
- Repo-wide `bun test` on merged main after all 8: only the pre-existing
  studio-desktop electron/bun-PATH environment failures (confirmed by running
  that package alone - same 4 fail/4 error count). No cross-chunk interaction
  this wave; the file-locality grouping held.

## Ops notes

- Codex homebrew self-update (0.147 -> 0.149) killed audit lanes mid-run twice;
  the DEAD-respawn path handled it (respawn on new binary, partial work kept).
- The zsh `for n in $VAR` no-word-split trap recurred in the monitor - fixed
  again with a literal name list (see fleet-orchestration-harness-lessons).
- v0.40.1 tag build stuck in the GitHub runner backlog (0 assets); v0.40.2
  supersedes it and install.sh's latest-fallback (#418) covers the gap.
