# Superpowers records

This directory contains work records. It does not contain current product guidance.

- `plans`, `specs`, `goals`, and `fleet-runs` are historical records.
- `research` contains evidence.

Use `docs/README.md` to find current guidance.
