# Ingest-speed hypothesis queue (work top-down; skip if superseded)

Profile (warm, concurrency=2): turn-content-blocks 30s · claude 19s · git 17s · turn-analysis 9s · rest small.
Each attempt = ONE change, benchmarked via `~/.cache/ax-bench/bench-run.sh <runs> <out.json>`, gated against `~/.cache/ax-bench/golden.json` counts, kept (commit on perf/ingest-speed) or reverted.

Status legend: `[ ]` todo · `[x]` done (see ATTEMPTS.md) · `[~]` superseded.

- [x] **002 - turn-content-blocks incremental via content_hash Map.** KEPT. warm 27s (vs 50s golden, -46%), cold 24s; counts exact-match golden. Indexed `content_document_source` lookup → `Map<source_ref, content_hash>`; UPSERT only new/changed hashes, no blanket DELETE; `AX_REDERIVE_CONTENT=1` escape hatch. Load existing `(turn → content_hash)` in ONE query (`SELECT source, content_hash FROM content_document WHERE source_kind='turn'`) into a JS Map; build/UPSERT only turns whose `stableDigest(text)` is new/changed; drop the blanket DELETE. NO `NOT IN` (attempt 001 proved it's unindexed + 3× slower). File: `apps/axctl/src/ingest/turn-content-blocks.ts`. Escape hatch: `AX_REDERIVE_CONTENT=1` forces full. Expected: kills the 30s on warm.
- [x] **003 - turn-analysis incremental.** KEPT. warm 24s (vs 27s best, -11%), cold 26s; all golden `.counts` exact-match. Turns are append-only ⇒ an existing `turn_analysis` row is output-equivalent (turn text/role/kind immutable; reacts_to/expresses edge ids deterministic). Load analyzed turn keys in ONE indexed read (`SELECT turn FROM turn_analysis`, UNIQUE `turn_analysis_turn` index) → JS `Set`; derive over the full ordered row set (reacts_to "previous assistant turn" lookahead needs full session context) but persist only turns not yet analyzed; dropped the blanket DELETE. `AX_REDERIVE_ANALYSIS=1` escape hatch forces full reset+re-derive. Output-equivalence verified: forced full re-derive on the post-bench DB reproduced the identical turn_analysis 15201 / semantic_signal 21 / expresses 438 / reacts_to 45. File: `apps/axctl/src/ingest/turn-analysis.ts`.
- [x] **004 - PIPELINE_CONCURRENCY 2 → 4.** KEPT. warm 22s (vs 24s best, -8%), cold 22s; all golden `.counts` exact-match. One-line constant bump in `apps/axctl/src/ingest/stage/runner.ts`. No DB-contention regression at the bench scale (per-stage internal fan-out stays unchanged; only the count of stages allowed inside the semaphore concurrently rose). Pure measurement knob - no correctness surface touched.
- [x] **005 - executeStatements chunkSize 250 → 1000.** REVERTED. cold 20s / warm 23s (≥ 22s best); counts exact-match golden but no warm win. Bumped both turn-content-blocks callers (the canonical 250-chunk hot stage) 250→1000. Because turn-content-blocks is incremental since attempt 002, warm runs write only new/changed-hash turns (near-no-op) ⇒ chunk size only affects the cold full-derive path; cold 20s sat inside bench noise of the 22s best. The now-dominant warm stages (claude/git, ~19s/17s) already use chunkSize 500, and their cost is per-record round-trips (per-commit queries in git, RELATE loops in claude), not chunk count - so a chunk bump there wouldn't move warm either. Move to 006/007 (skip-unchanged), which attack those stages structurally. File: `apps/axctl/src/ingest/turn-content-blocks.ts`.
- [x] **006 - claude skip-unchanged source files.** KEPT. cold 21s / warm 13s (vs 22s best, **-41%**); all golden `.counts` exact-match. New `ingest_file_state` table (path UNIQUE + mtime_ms/size, `source_kind`) holds a per-file watermark; `ingestTranscripts` loads it in ONE indexed read into a JS `Map<path,{mtime,size}>`, always `stat`s each candidate, and SKIPS parse+write for any file whose (mtime,size) still matches (rows already persist ⇒ output-equivalent), then upserts the watermark only after every write for that file succeeds. NO `NOT IN`. `AX_REDERIVE_CLAUDE=1` escape hatch. Runs [21,20,13] (run 1 cold builds watermarks, 2-3 skip unchanged). Files: `apps/axctl/src/ingest/transcripts.ts`, `packages/schema/src/schema.surql`.
- [x] **007 - git stage skip-unchanged.** KEPT. cold 24s / warm 8s (vs 13s best, **-38%**); all golden `.counts` exact-match. Reused `ingest_file_state` (`source_kind='git_repo'`, `path`=repo root, `sha`=HEAD, `since_days`=window). `ingestGit` loads all git watermarks in ONE indexed read into a JS `Map<path,{sha,sinceDays}>`; per repo, if `HEAD == sha && sinceDays == since_days` the repo is unchanged ⇒ commits/files already persist ⇒ SKIP `fetchCommits` (the `git log` + per-commit `git show --numstat` walk) and pass `commits=[]` to `writeRepo` (cheap repo/checkout/session-link upserts still run). After a real walk, UPSERT the watermark to HEAD; a new commit (HEAD moves) or a wider `since_days` forces a fresh walk. NO `NOT IN`. `AX_REDERIVE_GIT=1` escape hatch. Schema: `mtime_ms`/`size` → `option<number>` + new `sha`/`since_days` so one table serves both source kinds. Runs [24,21,8] (run 1 cold builds watermarks, 2-3 skip). Files: `apps/axctl/src/ingest/git.ts`, `packages/schema/src/schema.surql`.
## Re-profile @ warm 8s (concurrency=4), attempt 008
Per-stage SpanEnd durationMs (warm, single --debug run):
closure 3403 · subagents 2071 · pricing 1637 · outcomes 1629 · codex 1010 · classifier-results 706 · session-health 492 · turn-content-blocks 462 · git 444 · signals 408 · reaction-events 368 · turn-analysis 286 · proposals 268 · harness 250 · skills 91 · claude 53 · commands 51 · rest <30.
Wall ~8s (concurrency=4 overlaps these). New top stage = **closure** (3.4s).

Closure is the classic blanket-DELETE-then-full-re-derive antipattern (like 002/003 fixed): every warm run `DELETE later_fixed_by;DELETE suggests_skill;DELETE skill_candidate;DELETE commit_classification;` (1513 classifications + **6346 later_fixed_by** edges + 58 suggests_skill + 4 candidates) then re-RELATEs all 6346 edges - even when commit/touched/session_health are unchanged (git skip-unchanged means no new commits on warm). The 6346-edge DELETE + 6346-edge RELATE dominates the 3.4s.

- [x] **008 - closure skip-unchanged via input-fingerprint watermark.** KEPT. cold 25s / warm **6s** (vs 8s best, **-25%**); all golden `.counts` exact-match. Reuse `ingest_file_state` (`source_kind='closure'`, fixed sentinel path `__closure__`). After the (cheap) read of commits/touched/sessionHealth, compute `stableDigest` (32-hex) over the exact derive inputs (commit id|message|ts|repo, touched in|path, session_health fields) + sinceDays. Load the stored closure watermark in ONE indexed read; if the fingerprint matches the stored digest ⇒ inputs unchanged ⇒ output already persists ⇒ SKIP the blanket DELETE + full re-derive/re-write entirely (output-equivalent). On any change (or no watermark, or `AX_REDERIVE_CLOSURE=1`), do the full derive + DELETE + write, then UPSERT the watermark. NO `NOT IN`. No schema change (007's `sha` field already optional). Closure dropped 3403ms → negligible; output-equivalence verified via forced full re-derive reproducing identical commit_classification 1513 / later_fixed_by 6346 / suggests_skill 58 / skill_candidate 4. New top warm stage = subagents (~1.9s). File: `apps/axctl/src/ingest/closure.ts`.
- [ ] **009 - subagents (2.1s) / pricing (1.6s) / outcomes (1.6s)** incremental, if still material after closure win.
- [ ] **RocksDB cache/write-buffer bump** `AX_DB_ROCKSDB_BLOCK_CACHE_SIZE` etc (in `scripts/db-start.sh`). Constant across attempts once chosen.
- [ ] **010+ - re-profile** after each kept win; chase the new top stage. Add hypotheses as the bottleneck map shifts.

## Rules (every attempt)
- ONE change. Gate: all `golden.json` counts must match (output-equivalent) AND `bun run typecheck` clean AND the touched stage's unit test green. Faster-but-wrong = revert.
- Keep = commit on `perf/ingest-speed` with before/after in the message. Revert = `git restore`.
- Log: append a row to ATTEMPTS.md + write `bench-results/NNN.json`. Persist everything (resumable).
- Never touch :8521/:8522 or real `~/.claude`/`~/.codex` as a write target. One ingest at a time.
