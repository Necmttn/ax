{
  "name": "@ax/hooks-sdk",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "exports": {
    "./*": {
      "types": "./src/*.ts",
      "import": "./src/*.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "comment": "deps are PINNED, not catalog: - this package is consumed via a file: dependency from ~/.ax/hooks (outside the workspace), where catalog: cannot resolve. Keep versions in sync with the root workspaces.catalog.",
  "dependencies": {
    "effect": "4.0.0-beta.78"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
