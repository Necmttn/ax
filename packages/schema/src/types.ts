// Shared TypeScript types derived from the SurrealDB schema (schema.surql).
//
// The DDL itself ships via the `@ax/schema/schema.surql` export (imported with
// `{ type: "text" }`); this module is the home for hand-written types that mirror
// table shapes as consumers need them. Empty for now - add table row types here
// rather than redefining them per-consumer.
export {};
