# site-dist-trim plan

## Tasks
- Add a focused `bun:test` case proving `initForesight` only invokes a supplied devtools loader when `devtools` is enabled.
- Move the `foresight-devtools` dynamic import out of `packages/foresight/src/init.ts` and into the app dev-only call sites so production app builds can tree-shake it away.
- Build `apps/site` and `apps/studio`, then verify their `dist` outputs contain no emitted `foresight-devtools` or `lit` devtools chunk.
- Run the chunk gates and commit the finished work.

## Files
- `packages/foresight/src/init.ts`
- `packages/foresight/src/init.test.ts` or nearby package test file
- `apps/site/app/client.tsx`
- `apps/studio/src/main.tsx`

## Tests and Checks
- Red: `bun test packages/foresight/src/init.test.ts`
- Green: `bun test packages/foresight/src`
- Build checks: `bun run --cwd apps/site build`, `bun run --cwd apps/studio build`
- Dist grep checks: no `foresight-devtools` or devtools-lit chunk in either app `dist`
- Type checks: `bun run typecheck`, `bun run --cwd apps/site typecheck`

## Sequence
1. Inspect existing init and app call sites.
2. Write one failing test for devtools loader plumbing and confirm it fails.
3. Implement the minimal signature/call-site change.
4. Re-run package tests, then app builds and dist greps.
5. Run root and site typechecks.
6. Review git diff, `git add -A`, and commit with the required trailer.
