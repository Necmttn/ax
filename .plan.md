# fs-link-polish Plan

## Tasks
- Verify `ForesightLinkProps` has no repo-wide consumers outside `packages/foresight`.
- Add hash-aware key behavior tests for `stableKeyFrom`: distinct hashes produce distinct keys, and missing hash preserves existing key output.
- Implement hash inclusion in `stableKeyFrom` and update the `ForesightLink` call site.
- Remove the dead `ForesightLinkProps` export and any index re-export.

## Files
- `packages/foresight/src/foresight-link.tsx`
- Existing `packages/foresight/src/*foresight-link*.test*` test file
- `packages/foresight/src/index.ts`

## Test Sequence
1. Run grep proof for `ForesightLinkProps` consumers.
2. Write one failing hash behavior test and run the focused test file.
3. Implement the minimal source change and rerun the focused test file.
4. Remove the dead export and run package and app typechecks.
5. Run gates: `bun test packages/foresight/src`, `bun run --cwd packages/foresight typecheck`, `bun run --cwd apps/studio typecheck`, and root `bun run typecheck`.
6. Commit with the required co-author trailer.
