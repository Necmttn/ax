# fs-init-tests Plan

Goal: add happy-dom coverage for `initForesight` browser-only paths in `packages/foresight`.

Tasks:
- Add `happy-dom` as a dev dependency in `packages/foresight/package.json`, updating `bun.lock` and generated `bun.nix`.
- Create `packages/foresight/src/init.dom.test.ts` using `bun:test` and happy-dom globals.
- Test first:
  1. Assert a fresh browser import of `initForesight()` returns `true`, then `false` on the second call.
  2. Assert `dev: true` installs `window.__axForesight` and returns the ledger snapshot shape.
  3. Assert the real `callbackCompleted` listener path records success/error events visible through `window.__axForesight`.
- Run the failing test before implementation/dependency fixes, then make the minimal package/test support changes and rerun.
- Gates: `bun test packages/foresight/src`, `bun run --cwd packages/foresight typecheck`, and root `bun run typecheck`.
- Commit all changes with a conventional commit and the required `Co-Authored-By` trailer.
